export enum TransactionEnum {
  deposit = "deposit",
  withdraw = "withdraw",
  transfer = "transfer",
}

export enum TransactionTypeEnum {
  inSystem = "inSystem",
  outSystem = "outSystem",
}

export enum TransactionStatusEnum {
  pending = "pending",
  completed = "completed",
  failed = "failed",
}
